name: Build APK - Receiver

on:
  push:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Configurar JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Instalar Gradle 8.4
        uses: gradle/actions/setup-gradle@v3
        with:
          gradle-version: '8.4'

      - name: Gerar Gradle Wrapper
        run: gradle wrapper --gradle-version=8.4

      - name: Restaurar keystore de release
        env:
          RXR_KEYSTORE_B64: ${{ secrets.RXR_KEYSTORE_B64 }}
        run: |
          set -euo pipefail
          test -n "$RXR_KEYSTORE_B64" || { echo "RXR_KEYSTORE_B64 não configurado." >&2; exit 1; }
          printf '%s' "$RXR_KEYSTORE_B64" | base64 --decode > app/replayx-receiver-release.jks
          test -s app/replayx-receiver-release.jks
          echo "Keystore restaurada em caminho temporário."

      - name: Build APK (release)
        env:
          RXR_KEYSTORE_PATH: replayx-receiver-release.jks
          RXR_KEYSTORE_PASSWORD: ${{ secrets.RXR_KEYSTORE_PASSWORD }}
          RXR_KEY_ALIAS: ${{ secrets.RXR_KEY_ALIAS }}
          RXR_KEY_PASSWORD: ${{ secrets.RXR_KEY_PASSWORD }}
        run: |
          set -euo pipefail
          test -n "$RXR_KEYSTORE_PASSWORD" || { echo "RXR_KEYSTORE_PASSWORD não configurado." >&2; exit 1; }
          test -n "$RXR_KEY_ALIAS" || { echo "RXR_KEY_ALIAS não configurado." >&2; exit 1; }
          test -n "$RXR_KEY_PASSWORD" || { echo "RXR_KEY_PASSWORD não configurado." >&2; exit 1; }
          ./gradlew assembleRelease --no-daemon --stacktrace

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: ReplayX-Receiver-APK
          path: app/build/outputs/apk/release/app-release.apk
          retention-days: 30
